package com.opyate.tools.db

import math.ScalaNumber

/**
 * TODO Just use regular Java File stuff below. The scalax.io stuff isn't easy to use.
 *
 * @author Juan Uys
 */

object File extends App {
//  import scalax.io.{Codec, OpenOption}
//  import scalax.file.{Path, FileOps}
//
//  implicit val codec = scalax.io.Codec.UTF8
//  val file: FileOps = Path("file") //Path.createTempFile("ntd", "txt")
//
//  def write[T: Int](l: List[Int]) {
//    //file.write (l)
//    file.write (List (1,2,3) map (_.toByte))
//  }
//  file.write (List (1,2,3) map (_.toByte))
}
